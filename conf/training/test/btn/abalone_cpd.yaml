# @package _global_
# Auto-generated test config for BTN CPD on abalone
# Best config from ablation: L=3, prior=1.0, init=0.01, bond_dim=64

defaults:
  - /training/test/_base_uncertainty

method:
  bond_prior_alpha: 1.0

model:
  L: 3
  init_strength: 0.01
